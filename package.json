{
  "name": "rete-code-plugin",
  "version": "0.1.1",
  "description": "",
  "main": "build/code-plugin.min.js",
  "scripts": {
    "build": "rete --build rete.config.js",
    "build:dev": "rete --build rete.config.js --watch",
    "prepublishOnly": "npm run build"
  },
  "author": "Vitaliy Stoliarov",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/retejs/code-plugin.git"
  },
  "dependencies": {
    "case": "^1.5.5"
  },
  "devDependencies": {
    "rete-cli": "^0.2.0"
  }
}
